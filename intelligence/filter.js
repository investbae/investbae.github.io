"use strict";const form=document.getElementById("filter");form.addEventListener("submit",e=>e.preventDefault());const rows=[...document.querySelectorAll(".issue")];function apply(){const q=document.getElementById("query").value.trim().toLocaleLowerCase();const topic=document.getElementById("topic").value;const publisher=document.getElementById("publisher").value;let count=0;for(const row of rows){const show=(!q||row.textContent.toLocaleLowerCase().includes(q))&&(!topic||row.dataset.topic.split(" ").includes(topic))&&(!publisher||row.dataset.publisher===publisher);row.hidden=!show;if(show)count++;}document.getElementById("result-count").textContent=String(count)+" results";document.getElementById("empty").hidden=count!==0;}form.addEventListener("input",apply);form.addEventListener("reset",()=>setTimeout(apply,0));